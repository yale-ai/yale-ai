{
  "name": "yale-ai-association",
  "version": "0.1.0",
  "private": true,
  "description": "Yale Artificial Intelligence Association: website (web) and email campaigns (marketing)",
  "workspaces": [
    "web",
    "marketing"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "lint": "npm run lint --workspace web",
    "preview": "npm run preview --workspace marketing"
  }
}
